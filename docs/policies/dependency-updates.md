# Dependency update cadence
Assumes canonical model and terminology in `cardplay2.md` (repo root).

Suggested cadence:

- Weekly: patch updates for tooling
- Monthly: minor updates
- Quarterly: major updates (requires running `npm run check` and sanity testing)

