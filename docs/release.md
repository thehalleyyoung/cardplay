# Release
Assumes canonical model and terminology in `cardplay2.md` (repo root).

Cardplay is currently not set up for publishing. The main “release” flow is:

1. `npm run check`
2. `npm run build`
3. (Optional) `npm run docs:build`

